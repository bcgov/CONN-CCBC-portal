import TextAreaWidget from '../../../lib/theme/widgets/TextAreaWidget';

describe('TextareaWidget', () => {
  xit('simple with errors');
  xit('simple without errors');
  xit('simple with required');
  xit('simple without required');
  xit('returns the value in the input');
  xit('returns text as formatted without stripping of special chars');
});
