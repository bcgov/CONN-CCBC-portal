import { RJSFSchema } from '@rjsf/utils';

const map: RJSFSchema = {
  title: 'Map',
  description: '',
  type: 'object',
  required: [''],
  properties: {
    map: {
      type: 'string',
      title: null,
    },
  },
};

export default map;
